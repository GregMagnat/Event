<div class="container">
  <% if notice.present? %>
    <div class="alert alert-success"><%= notice %></div>
  <% end %>
  <h1 class="display-4">EVENTS</h1>

  <div class="row" id="events">
    <% @events.each do |event| %>
      <div class="col-md-4 mb-4">
        <%= render event %>
        <p class="text-center">
          <%= link_to "Voir cette event", event, class: "btn btn-primary btn-view-event" %>
        </p>
      </div>
    <% end %>
  </div>

  <div class="mt-3">
    <%= link_to "Créer ton event", new_event_path, class: "btn btn-success" %>
  </div>

  <style>
    /* Ajoutez ces styles directement dans le fichier HTML */
    .btn-view-event {
      margin-top: 10px; /* Vous pouvez ajuster cette valeur selon vos besoins */
    }
  </style>
</div>
