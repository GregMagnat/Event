<!DOCTYPE html>
<html>

<head>
  <title>EventGregos</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css', media: 'all' %>
  <%= stylesheet_link_tag 'bootstrap.css', media: 'all' %>
  <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css', media: 'all' %>
  <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js', integrity: 'sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM', crossorigin: 'anonymous' %>
  <style>
    body {
      background-image: url('https://images8.alphacoders.com/881/881478.png');
      background-size: cover;
      background-position: center;
      height: 100vh;
      margin: 0;
      padding: 0;
      overflow-x: hidden;
      position: relative;
      padding-bottom: 60px; /* Ajustez cette valeur en fonction de la hauteur de votre footer */
    }

    .logo-container {
      position: absolute;
      top: -170px;
      left: -100px;
      z-index: 4;
    }

    .second-logo-container {
      position: absolute;
      bottom: -90px;
      right: -290px;
      z-index: 4;
    }

    .navbar-nav {
      margin-left: auto;
    }

    .navbar-nav .nav-item {
      margin-right: 30px;
    }

    .center-text {
      text-align: center;
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    .footer {
      text-align: center;
      position: fixed;
      bottom: 0;
      width: 100%;
      padding: 10px;
      background-color: #f8f9fa;
      z-index: 1000; /* Assurez-vous que le z-index est supérieur à celui des autres éléments */
    }
  </style>
</head>

<body data-turbo="false">

  <div class="logo-container">
    <img src="<%= asset_path('diable bleu.png') %>" alt="Logo" style="max-width: 60%;">
  </div>

  <nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <% unless user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Log in", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
        <% if user_signed_in? %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Profil", user_path(current_user), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Logout", destroy_user_session_path, class: "nav-link" %>
            </li>
          </ul>
        <% end %>
      </ul>
    </div>
  </nav>

  <div class="second-logo-container">
    <img src="<%= asset_path('diable bas.png') %>" alt="Second Logo" style="max-width: 60%;">
  </div>

  <%= yield %>

  <div class="footer">
    <p1 class="center-text">©L'évolution de la planète des singes</p1>
  </div>
</body>

</html>
